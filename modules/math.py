def math(number):
    # Picks the minimum or smallest number of the list of numbers, adds your number variable
    x = min(number, 5, 10, 25)
    # Picks the max or smallest number of the list of numbers, adds your number variable
    y = max(number, 5, 10, 25)

    print(x)
    print(y)

    # Does times your number variable by 20
    calculation = number * 20
    print (calculation)
